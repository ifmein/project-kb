[project]
name = "project-kb"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "click>=8.3.3",
    "nanoid>=2.0.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
